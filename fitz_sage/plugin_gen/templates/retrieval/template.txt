# TEMPLATE: Retrieval Strategy Plugin
#
# Retrieval plugins define how to search and rank results.
#
# REQUIRED FIELDS:
#   - name: Unique strategy identifier
#   - type: Strategy type (e.g., "dense", "sparse", "hybrid")
#   - Any strategy-specific configuration

name: "CHANGE_ME"
type: "dense"  # "dense" | "sparse" | "hybrid"

# Dense retrieval settings (vector similarity)
dense:
  enabled: true
  top_k: 10
  score_threshold: 0.0

# Sparse retrieval settings (keyword matching)
sparse:
  enabled: false
  top_k: 10
  algorithm: "bm25"

# Hybrid settings (combine dense + sparse)
hybrid:
  dense_weight: 0.7
  sparse_weight: 0.3
  fusion: "rrf"  # "rrf" (reciprocal rank fusion) | "linear"

# Reranking (optional)
rerank:
  enabled: false
  top_n: 5
