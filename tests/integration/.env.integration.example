# Environment variables for cloud cache E2E integration tests
# Copy this file to .env.integration and fill in your values
#
# Prerequisites:
# 1. fitz-ai-cloud API running (locally or remote)
# 2. Ollama running with:
#    - nomic-embed-text (embeddings, 768-dim)
#    - qwen2.5:1.5b or similar (LLM generation)
# 3. Test organization with starter+ tier (free tier can't use cache)

# Required: API key from Fitz Cloud dashboard
# Format: fitz_xxx... (get from https://dashboard.fitz-ai.cloud/api-keys)
FITZ_CLOUD_TEST_API_KEY=fitz_your_api_key_here

# Required: Organization encryption key (64-character hex string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: This key NEVER leaves your machine - it's used for local encryption only
FITZ_CLOUD_TEST_ORG_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Required: Organization ID (UUID format)
# Get from Fitz Cloud dashboard or organization settings
FITZ_CLOUD_TEST_ORG_ID=00000000-0000-0000-0000-000000000000

# Optional: Cloud API base URL
# Default: http://localhost:8000/v1 (for local development)
# Production: https://api.fitz-ai.cloud/v1
FITZ_CLOUD_URL=http://localhost:8000/v1

# Run tests with:
#   source tests/integration/.env.integration
#   pytest tests/integration/test_cloud_cache_e2e.py -v
#
# Or set variables inline:
#   FITZ_CLOUD_TEST_API_KEY=fitz_xxx FITZ_CLOUD_TEST_ORG_KEY=xxx FITZ_CLOUD_TEST_ORG_ID=xxx pytest tests/integration/test_cloud_cache_e2e.py -v
