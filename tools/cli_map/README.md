# tools/cli_map/

Generates the CLI command reference used in `docs/CLI.md`.

Introspects Click command groups registered under `fitz_sage/cli/` and emits structured documentation.

## Usage

```bash
python -m tools.cli_map
```
